"use strict";//treat all js files as newer version

// alert("Hello World!");//we use nodejs, not browser, so alert() won't work

console.log(3 
    +
     3);// code readablitiy should be high

console.log("LUKMAN");

let name = "Lukman";//string
let age = 20;//number
let isApproved = false;//boolean
let latName;//undefined
let selectedColor = null;//null

console.log(typeof null);//object
console.log(typeof lastName);//undefined